# Biofetch CLI v2, InterProScan, and AO Adoption Closeout

Version: v1.0
Date: 2026-07-24
Status: archived; implementation complete

## Scope

This closeout records completion of the
[approved implementation plan](20260723-v1.0-cli-v2-interpro-scan-ao-adoption-implementation-plan.md).
Maintained behavior remains documented through the
[current documentation map](../README.md); this file is historical delivery
evidence, not an implementation authority.

## Delivered Pull Requests

- [PR #7](https://github.com/FuqingZh/biofetch/pull/7),
  `docs: add AO repository intake guide`, merged as
  [`801466c`](https://github.com/FuqingZh/biofetch/commit/801466c62f22f67182966df65866a914cec39530).
- [PR #8](https://github.com/FuqingZh/biofetch/pull/8),
  `feat(cli): migrate all commands to v2 contract`, merged as
  [`681921d`](https://github.com/FuqingZh/biofetch/commit/681921de81f846c59811e1af731e11936577dd6a).
- [PR #9](https://github.com/FuqingZh/biofetch/pull/9),
  `feat(interpro): add InterProScan distribution workflow`, merged as
  [`3e536bf`](https://github.com/FuqingZh/biofetch/commit/3e536bf47196cba6c9c8da2a829c81fa9fa28d2c).

Final GitHub readback showed `validate-biofetch` successful on all three pull
requests. PR #7 had no review threads; all seven threads on PR #8 and all eight
threads on PR #9 were resolved, leaving zero unresolved threads. Auto-merge
was disabled throughout.

## Review Continuation

Automatic Codex Review returned real anchored findings to the original AO
workers, which corrected, verified, and pushed the affected implementations.
The CLI continuation corrections:

- preserved combined inline and file-list values regardless of flag order;
- applied the default manifest format only when no format input was supplied;
- restored KEGG request throttling;
- rejected removed implicit `@file` syntax;
- validated snapshot asset and database identity; and
- accepted valid relative snapshot operands.

The InterProScan and shared static-asset continuation corrections:

- populated official URLs when locking a scan snapshot;
- made corrupt completed partial downloads recoverable;
- removed successful chunk-download workspaces;
- rejected partial scan manifests during restore;
- prevented invalid or replaced checksum files from being published;
- excluded unmatched scratch files from fixed-asset locks; and
- preserved a reused final file until its verified replacement succeeded.

Fresh remote readback after the corrections confirmed successful CI and zero
unresolved review threads.

## AO Acceptance

The accepted AO service reported ready. A final `ao doctor --json` run returned
`"ok": true` with zero failures, including a successful GitHub token probe.
The real review-continuation corrections above completed the planned AO canary:
the original workers received actionable review findings and pushed verified
repairs. Merge and risk decisions remained with the user, and auto-merge
remained disabled.

## Live EBI InterProScan Acceptance

The approved temporary acceptance root completed two live EBI fetches for
InterProScan `5.77-108.0`. The first fetch downloaded and verified the official
archive and checksum; the second fetch reused the verified results.

Recorded archive evidence:

```text
bytes: 7042879506
md5: 9526f10d0b8d8f979996a0696d78c7dd
sha256: 9875a47c392201681be9bbfcfad8d989ff63ff63e77b5e6a035117faa0ab6752
```

After acceptance, temporary download state was absent: no archive `.part` file
or chunk workspace remained.

## Final Result

The repository now exposes the approved CLI v2 contract, versioned
InterProScan fetch/lock/restore workflow, and repository-owned AO delivery
entrypoint. Current architecture and testing documents contain the maintained
contracts, so the completed implementation plan has been removed from active
authority and archived with this closeout.
